# Backend API as seen from the Next.js server
LEASELENS_API_URL=http://localhost:8080
# Backend API as seen from the browser (search-as-you-type); must be allowed by the backend's CORS
NEXT_PUBLIC_LEASELENS_API_URL=http://localhost:8080
